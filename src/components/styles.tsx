import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(theme => ({
    graph: {
        marginTop: 100,
    },
    summery: {
        display: "flex",
        justifyContent: "center",
        width: 300,
        marginLeft: 200,
        marginTop: 10,
    },
    root: {
        width: '100%',
    },
}));